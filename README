If the Make file returns linking errors originating from volume.c, to fix this issue the libasound2-dev libary will need to be installed.

To compile gstreamer the following tools and libraries need to be installed:

pkg-config 
libtool
gstreamer library